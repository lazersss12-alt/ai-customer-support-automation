NEXT_PUBLIC_N8N_CHAT_WEBHOOK_URL=

# Server-only — never prefix with NEXT_PUBLIC_ (would expose it to the browser)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# HTTP Basic Auth gate for /dashboard
DASHBOARD_USERNAME=
DASHBOARD_PASSWORD=
